mutation DeleteBook($id: String!) {
  deleteBook(id: $id) {
    book {
      author
      createdAt
      id
      publicationYear
      title
    }
    error {
      code
      message
    }
    success
  }
}
